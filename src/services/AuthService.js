import auth from '@react-native-firebase/auth';

const AuthService = {
  signInWithGoogle: async () => {
    // Firebase Google Sign-In logic
  },
  signInWithPhoneNumber: async (phoneNumber) => {
    // Firebase Phone Number login logic
  }
};

export default AuthService;
